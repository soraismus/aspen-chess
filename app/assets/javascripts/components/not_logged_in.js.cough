{ div, h1, h2, span } = require './DOM'
SignupButton          = require './signup_button'

NotSignedIn = ->
  div
    className : 'center hero-unit'  # Bootstrap aspect.
    h1
      irrelevant : null
      'Welcome to '
      span
         className : 'app-title' 
        'Chessplay'
        '.'
    h2
      irrelevant : null
      "Matthew Hilty's Rails demo application"
    SignupButton ()

module.exports = NotSignedIn
