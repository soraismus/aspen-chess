require './login_form'
require './logout_form'
require './signup_form'

Home = require './home'

form? = \view ->
  /_form/.test view

Main = \props ->
  { csrf, home, view } = props
  
  if form? view
    component = require "./#{view}"
    component csrf
  else
    Home home
 
module.exports = Main
