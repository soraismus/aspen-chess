Home        = require './home'
SigninForm  = require './signin_form'
SignupForm  = require './signup_form'

Main = \props ->
  { csrf, home, view } = props

  switch view
    when 'signin' then SigninForm csrf
    when 'signup' then SignupForm csrf
    else               Home home
 
module.exports = Main
