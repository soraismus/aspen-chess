require './home'
require './login_form'
require './logout_form'
require './signup_form'

form? = \view ->
  /_form/.test view

Main = \props ->
  { csrf, home, view } = props

  component = require "./#{view}"
  component (if form? view then csrf else home)
 
module.exports = Main
