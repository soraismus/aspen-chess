Home       = require './home'
LoginForm  = require './login_form'
SignupForm = require './signup_form'

Main = \props ->
  { csrf, home, view } = props

  switch view
    when 'login'  then LoginForm csrf
    when 'signup' then SignupForm csrf
    else               Home home
 
module.exports = Main
