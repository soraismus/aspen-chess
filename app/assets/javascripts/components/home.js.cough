NotLoggedIn = require './not_logged_in'
LoggedIn    = require './logged_in'

Home = \props ->
  { logged_in, user_home_view } = props

  if logged_in
    LoggedIn user_home_view
  else
    NotLoggedIn ()

module.exports = Home
