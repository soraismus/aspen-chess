NotLoggedIn = require './not_logged_in'
# LoggedIn    = require './logged_in'

Home = \props ->
  { logged_in } = props
  NotLoggedIn ()

###
Home = \props ->
  { logged_in } = props

  if logged_in
    LoggedIn home_logged_in
  else
    NotLoggedIn ()
###

module.exports = Home
