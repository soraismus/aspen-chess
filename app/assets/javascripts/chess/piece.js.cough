chessmanFns                            = require './chessman'
{ defaults }                           = require './base-helpers'
{ unsetPassantPos, unsetPromotionPos } = require './constants'

setPassantPosition = ->
  unsetPassantPos

setPromotionPosition = ->
  unsetPromotionPos

pieceFns = defaults chessmanFns {
  setPassantPosition
  setPromotionPosition
}

module.exports = pieceFns
